

<h1><%= @article.title %></h1>
<p><%= @article.content %></p>


<p><%= link_to 'Edit', edit_article_path(@article) %></p>
<%= button_to 'Destroy article', article_path(@article), method: :delete, data: { confirm: 'Are you sure?' } %>
